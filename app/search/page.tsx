'use client'
import React from 'react'
import SearchBar from '../ui/SearchBar'

type Props = {}

const page = (props: Props) => {
  return (
    <div>
        <SearchBar />
    </div>
  )
}

export default page